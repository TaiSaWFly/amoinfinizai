import { cusromizeImageList } from "../data/mockData";

const useAppImage = () => {
    const images = {
        unleashGamingImage: "assets/images/unleash_gaming_image.webp",
        customizeImage: "assets/images/custimize_image.webp"
    };

    return { images, cusromizeImageList };
};

export default useAppImage;
